# Local DSH test home (sessions, storages, healed profile node_modules)
.dsh-home/
node_modules/
# Local npm cache used when ~/.npm is read-only in the sandbox
.npm-cache/
# Research scratch (e.g. upstream deepseek-harness clone for patch specs)
.tmp-check/
# Upstream deepseek-harness source clone (read-only reference, not part of this repo)
deepseek-harness/
