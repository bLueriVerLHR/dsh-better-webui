{
  "name": "@blueriverlhr/dsh-better-webui-search",
  "description": "Web GUI keyless Exa web search: registers an `exa` provider on the ctx.web seam so the native web_search tool works without an API key (anonymous Exa hosted MCP by default, REST upgrade via EXA_API_KEY). Host only.",
  "version": "0.14.0",
  "private": true,
  "type": "module",
  "main": "lib/index.js",
  "exports": {
    ".": "./lib/index.js",
    "./cordis.patch.yml": "./cordis.patch.yml",
    "./package.json": "./package.json"
  },
  "files": [
    "lib/",
    "cordis.patch.yml",
    "README.md"
  ],
  "dsh": {
    "bundle": {
      "patch": "./cordis.patch.yml"
    }
  },
  "scripts": {
    "build": "node ../../scripts/build-package.mjs ."
  },
  "license": "GPL-3.0"
}
